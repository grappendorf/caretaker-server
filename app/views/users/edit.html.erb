<% provide :title, "Edit #{@user.name}" %>

<div class="row">
  <% if @myself == :profile || @myself == :password %>
    <div class="col-md-6 col-md-offset-3">
      <%= render 'form' %>
    </div>
  <% else %>
    <div class="col-xs-12 col-sm-10 col-md-8 col-lg-6">
      <%= render 'form' %>
    </div>
  <% end %>
</div>
